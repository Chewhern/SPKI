# Path
You can change the path if you want to, this is up for you to config. Any configuration or changes made to path won't trigger AGPL, this includes client side applications.

# Duration
You can change the duration of "challenge" and the valid duration of node's, node's authorized user and signed records. Such changes won't trigger AGPL.  
