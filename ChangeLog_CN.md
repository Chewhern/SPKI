# 版本 0.0.0-Alpha
- 通过模仿 CA 分层模型构建私有 PKI 基础设施。
- 严格使用可变数据类型进行安全内存处理。
- 在服务器端创建并使用了轻量级的自制软件 HSM 概念。
- 如果使用带外通信（如 Signal Messenger/WhatsApp Messenger 安全号码中所述），则可以防止中间人攻击 (MITM)。
- 将签名 + 信任操作与加密或密码学操作分离。
- 所有节点级别的身份验证操作都依赖于公钥身份验证。
- 所有从用户到节点的身份验证操作也依赖于公钥身份验证。

## 缺点/限制
- 当涉及授权用户的私钥时，需要在服务器端手动更新和轮换密钥。
- 一旦达到到期日期，授权用户和最终用户的签名记录将被自动删除。
- 日期时间解析存在问题，需要时间修复。
- 密钥提交问题和未经授权更改日志数据是目前无法解决的两个问题。
- 软件 HSM 概念尚不完善 **(将在 0.0.1 版本中描述)**

# 0.0.1-Alpha 版本
与 0.0.0 版本相同，但增加了基于软件 HSM + KMS 的基本功能，包括**身份验证、授权和记账**。

客户端将负责选择要上传到服务器端的密钥。

只有当客户端的公钥获得该基础设施的顶级和中级授权用户签名后，客户端才能使用此新功能。

## 缺点/限制
日期时间解析问题相同。

在 Windows、MacOS 和 Linux（也可套用于Android/iOS）中，尽可能避免将敏感数据交换到磁盘始终是一个好主意。
因此，必须使用 libsodium 的 **mlock**。
使用该函数时存在某些限制或困难。
最好的办法是使用 libsodium 中的 **malloc**，它会对其返回的指针应用 **mlock**。
这解决了交换问题。
只是使用指针需要添加更多函数/增强我创建的现有 **ASodium** C# 库，使其能够接受指针数据类型，而不是当前的 byte[] 数据类型。
